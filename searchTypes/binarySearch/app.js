/**
 * Binary search and calculation of it's time
*/

let arr = [10, 20, 30, 40, 50, 60, 70];

let toBeFoundNumber = 20;

function binarySearch(arr, num){
    
    let center = 10

    return -1;
}

console.time('lab');
console.log(binarySearch(arr, toBeFoundNumber));
console.timeEnd('lab');

